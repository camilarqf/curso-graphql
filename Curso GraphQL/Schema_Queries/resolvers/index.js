const Query = require("./Query");
const Produto = require("./Produto");
const Usuario = require("./Usuario");

/* exporta as const para o index para serem carregados no index.js principal */

module.exports = {
  Query,
  Produto,
  Usuario,
};
